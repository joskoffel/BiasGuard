/**
 * PostCSS configuration enabling Tailwind CSS and Autoprefixer. Tailwind CSS
 * generates utility classes based on the content of your project; see
 * `tailwind.config.js` for customisations.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};